Drop a painted background here to replace the built-in SVG courtyard:

  mehfil.jpg          landscape, used on screens wider than 640px  (1600x900 or larger)
  mehfil-mobile.jpg   portrait,  used on phones                    (940x1670 or larger)

Both are optional. If neither file exists the page silently falls back to the
illustrated haveli scene drawn in the HTML, so nothing breaks.

Keep them dark and low-contrast in the middle — the wordmark and the sher sit
on top of that area.
